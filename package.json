{
  "name": "devops-pulumi-ts",
  "version": "1.0.0",
  "description": "Pulumi-based infrastructure for deploying containerized apps to GCP Cloud Run or Azure Container Apps",
  "private": true,
  "scripts": {
    "test": "cd cli && npm test",
    "test:cli": "cd cli && npm test",
    "lint": "cd cli && npm run lint",
    "build": "cd cli && npm run build",
    "typecheck": "cd bootstrap && npx tsc --noEmit && cd ../infrastructure && npx tsc --noEmit && cd ../app && npx tsc --noEmit && cd ../cli && npx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
